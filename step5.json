{
  "commands": [
    {
      "call": "replace_by_content",
      "title": "替换list_on_mouse_down中的UIEventDispatcher调用（第一处）",
      "args": {
        "file": "/home/wulin/myopenwarcraft3/src/ui/ui_list.c",
        "old_str": "            if (event->mouse.y >= thumb_y && event->mouse.y < thumb_y + thumb_height) {\n                // 点击了拇指，开始拖动\n                list->is_dragging_scrollbar = true;\n                list->scrollbar_drag_start_y = event->mouse.y;\n                list->scrollbar_drag_start_offset = list->scroll_offset;\n                // 捕获鼠标，防止鼠标移出范围后丢失事件\n                if (list->dispatcher) {\n                    UIEventDispatcher_CaptureMouse(list->dispatcher, component);\n                }\n                return true;",
        "new_str": "            if (event->mouse.y >= thumb_y && event->mouse.y < thumb_y + thumb_height) {\n                // 点击了拇指，开始拖动\n                list->is_dragging_scrollbar = true;\n                list->scrollbar_drag_start_y = event->mouse.y;\n                list->scrollbar_drag_start_offset = list->scroll_offset;\n                // 捕获鼠标，防止鼠标移出范围后丢失事件\n                if (list->scene_manager) {\n                    SceneManager_CaptureMouse(list->scene_manager, component);\n                }\n                return true;",
        "startLine": 225
      }
    },
    {
      "call": "replace_by_content",
      "title": "替换list_on_mouse_down中的UIEventDispatcher调用（第二处）",
      "args": {
        "file": "/home/wulin/myopenwarcraft3/src/ui/ui_list.c",
        "old_str": "            } else {\n                // 点击了滚动槽，跳转到点击位置\n                float click_ratio = (event->mouse.y - component->y) / track_height;\n                int new_offset = (int)(click_ratio * max_scroll + 0.5f);\n                if (new_offset < 0) new_offset = 0;\n                if (new_offset > max_scroll) new_offset = max_scroll;\n                list->scroll_offset = new_offset;\n                // 捕获鼠标，防止鼠标移出范围后丢失事件\n                if (list->dispatcher) {\n                    UIEventDispatcher_CaptureMouse(list->dispatcher, component);\n                }\n                return true;",
        "new_str": "            } else {\n                // 点击了滚动槽，跳转到点击位置\n                float click_ratio = (event->mouse.y - component->y) / track_height;\n                int new_offset = (int)(click_ratio * max_scroll + 0.5f);\n                if (new_offset < 0) new_offset = 0;\n                if (new_offset > max_scroll) new_offset = max_scroll;\n                list->scroll_offset = new_offset;\n                // 捕获鼠标，防止鼠标移出范围后丢失事件\n                if (list->scene_manager) {\n                    SceneManager_CaptureMouse(list->scene_manager, component);\n                }\n                return true;",
        "startLine": 235
      }
    }
  ]
}
