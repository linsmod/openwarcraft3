{
  "commands": [
    {
      "call": "replace_by_content",
      "title": "删除重复的注释",
      "args": {
        "file": "/home/wulin/myopenwarcraft3/src/html/layout.c",
        "old_str": "    // Set the mutable rect output data to the starting input data\n    ctx->rects[item][dim] = pitem->margins[dim];\n\n    // If we have an explicit input size, just set our output size (which other\n    // calc_size and arrange procedures will use) to it.\n    // If we have an explicit input size, just set our output size (which other\n    // calc_size and arrange procedures will use) to it.\n    // Note: The explicit size should be to total size including content + padding + border\n    if (pitem->size[dim] != 0) {",
        "new_str": "    // Set the mutable rect output data to the starting input data\n    ctx->rects[item][dim] = pitem->margins[dim];\n\n    // If we have an explicit input size, just set our output size (which other\n    // calc_size and arrange procedures will use) to it.\n    // Note: The explicit size should be total size including content + padding + border\n    if (pitem->size[dim] != 0) {"
      }
    }
  ]
}
